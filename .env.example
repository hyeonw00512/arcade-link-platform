PORT=3000
CLIENT_ORIGIN=*
PUBLIC_APP_URL=http://localhost:3000
SESSION_TTL_DAYS=30
# 32자 이상 임의 문자열. Render 환경 변수에도 같은 값을 등록하세요.
PLATFORM_JOIN_SECRET=replace-with-a-long-random-secret
# 운영 환경에서는 Neon, Supabase 또는 Render PostgreSQL 연결 문자열을 넣으세요.
# 비어 있으면 개발용 JSON 파일 저장을 사용합니다.
DATABASE_URL=
